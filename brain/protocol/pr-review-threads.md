# PR review threads

As the author (or author-side agent), reply and fix — do **not** resolve review conversations.

- `Resolve conversation` / thread resolve is the reviewer's job.
- If a reply or UI action left a thread resolved, unresolve it.
- Leave resolve / unresolve judgment to the reviewer who raised the point.
